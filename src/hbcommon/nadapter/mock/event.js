/**
 * 手机事件监听
 */

export default {
  addEventListener(event, callback, capture) {
  },

  removeEventListener() {
  },

  /**
   * 监听网络变化
   */
  netchange(callback) {
  },

  // 监听返回按钮时间
  listenBack(callback) {
  }
}